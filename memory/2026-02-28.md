# 2026-02-28 작업 일지

## 로그아웃 버그 수정 (08:00)
- **문제**: `forceLoginRedirect()`가 logout API를 `await` 없이 호출 → 쿠키 삭제 전 리다이렉트
- **수정**: `handleSignOut`에서 `nukeAllAuthState()` → `await fetch('/api/auth/logout')` → `window.location.replace('/login')` 순차 실행
- **커밋**: `772f86b` fix: 로그아웃 - logout API await 후 리다이렉트
- **배포**: Vercel 프로덕션 완료
- **잔여 이슈**: `forceLoginRedirect()` 자체(context.tsx 102, 189줄)는 여전히 non-await 패턴 — 추후 수정 필요

## 관리자 지시사항 (영구 기억)

### 1. 백그라운드 작업 → 서버(Vercel 등)로 이전
- 개발/대화 제외한 모든 백그라운드 작업은 Vercel Cron 등 서버에서 구동
- PM2 스케줄러 → Vercel Cron으로 이전 필요 (onda-logic-monitor)
- 이유: 토큰 만료 빈도 줄이기

### 2. 아이보스 + 셀클럽 커뮤니티 크롤러
- 아이보스(i-boss.co.kr) + 셀클럽(sellclub.co.kr) 크롤링
- 하루 1회 실행 (Vercel Cron 매일 06:00 KST)
- 로그인 불필요 (둘 다 비로그인 열람 가능)
- 별도 탭 X → 기존 로직모니터 데이터에 통합
  - 경고센터: 커뮤니티 시그널 → alerts 자동 삽입
  - 업체 상세 모달: "커뮤니티 동향" 섹션
  - AI 분석: 커뮤니티 추측 데이터 반영
  - 클라이언트 포털: "최근 업계 동향" 표시
- 추측성 데이터로 넣고 실제 크롤링 데이터와 교차 검증

### 3. 할 수 있는 건 직접 하고, 안 되면 그때 요청
- Supabase 테이블 생성 등 직접 가능한 작업은 먼저 시도
- 안 될 때만 사용자에게 요청
- 요청 먼저 하지 말 것

### 4. 요청 내용 전부 기록
- 관리자가 말한 내용 전부 md + git 커밋 + 메모리 저장
- 한 번 말한 거 다시 안 물어보게 영구 기억

## 작업 완료

### 미커밋 파일 정리 (3개 프로젝트)
- onda-coldmail: 12파일 → main 푸시 ✅
- onda-hompage: 13파일 → feature 브랜치 푸시 ✅ (draft-1~10 submodule 경고)
- onda-test: 6파일 → main 푸시 ✅

### 커뮤니티 크롤러 구현 (onda-logic-monitor)
- crawler/iboss.ts, crawler/sellclub.ts 생성
- lib/ai/community-signal.ts 생성
- app/api/cron/community-crawl/route.ts 생성
- Supabase community_signals 테이블 생성 (Management API로 직접 실행)
- Vercel 배포 완료: https://onda-logic-monitor.vercel.app

### 로직모니터 전면 재설계 (대규모)
- AI 엔진 4개 신규: benchmark, rotation, anomaly, cost-optimizer
- 직원 대시보드 재설계: 투입추천/결제D-day/매체효율/2주 로테이션
- 고객 포털 재설계: 순위/경쟁사/25일보장/주간리포트/하락원인 자동생성
- 대행사 포털 구현: 다중 업체 관리, 레시피 비공개
- DB 테이블 6개 생성: source_rotation_schedule, daily_recommendations, logic_tests, weekly_reports, contract_tracking, source_efficiency
- Cron 5개: crawl-next(14-18시KST), post-crawl-analysis(19시), staff-guide(20시), weekly-report(일요일), crawl-exploratory
- AdLog 광범위 크롤링: crawlExploratoryKeyword 메서드 추가
- BUSINESS_CONTEXT.md 생성 (사업 컨텍스트 전체 기록)

### 성능 최적화
- 대시보드 DB 호출: 120회 → 3회 (배치 쿼리)
- 클라이언트 포털: 50회 → 6회
- 대행사 포털: 50회 → 4회
- 인증 캐싱: fetchProfile 결과 sessionStorage 5분 캐시

### 로그인/로그아웃 수정 (여러 차례)
- 로그아웃: await 제거 → 즉시 캐시삭제 + window.location.replace
- 3초 타임아웃 보험 추가
- 전체 레이아웃 4개 (dashboard/admin/client/agency) 동일 수정
- 루트 / → /login 리다이렉트로 변경
- /login?logout=1 시 세션 즉시 파괴
- 로그인 페이지 로딩 스피너 최대 1초 → 무조건 폼 표시
- tsconfig es5 → es2017 (Set iteration 에러 해결)

### OpenClaw 인증 설정
- Max 요금제 토큰(setup-token)만 사용하도록 변경
- API 키 폴백 제거 (api-key를 order에서 삭제)
- 관리자가 SSH에서 openclaw auth login + systemctl restart 실행 완료

### 관리자 추가 지시 (영구)
- 작업완료 = 테스트+디버깅 무한반복 후 완전 동작 확인. 사용자에게 테스트 떠넘기지 말 것.
- 디자인: 모바일 퍼스트, 모든 기기 호환, 직관적 가독성
- 로그인 방식: AdLog 스타일 (오류 시 자동 재로그인)

## 서버 현황
- 서버 위치: 핀란드 (FI) — 한국 IP 아님
- 현재 유저: onda (sudo 권한 없음, 컨테이너 no-new-privileges)
- 관리자가 권한 열어주기로 함 (아직 미완료)

## onda-logic-monitor 로그인 이슈 조사 (진행중)
- 사용자 "000" 로그인 안 됨 보고
- Supabase 서버사이드 인증은 정상 (user ID: 93217ff2-466e-4ab8-80cc-71cba4421e9f)
- 원인 추정: `app/login/page.tsx`의 `nukeCache()` 함수가 로그인 직전(line 106)에 localStorage 삭제 → Supabase auth 방해
- `nukeCache()`+`signOut()` 조합이 lines 68, 82에서도 호출됨 → abort 에러 유발 가능
- 이전에도 abort 에러 문제로 signOut 제거한 이력 있음 (commit ae56c22, 44ae6a4)
- 다음: nukeCache 타이밍 수정 → 빌드/배포 → 로그인 확인

## Claude 플랜 사용량 제어 시스템 (17:28)
- `/home/onda/scripts/claude-throttle.sh` 생성
- 사용량 0-60%: parallel 모드 (병렬 허용)
- 사용량 60-85%: single 모드 (단일 세션만, 나머지 큐잉)
- 사용량 85%+: wait 모드 (리셋 대기, 작업 큐에 저장)
- 리셋 시간 감지 → 큐 자동 처리
- 모든 프로젝트에서 `claude-throttle.sh run` 으로 호출하면 자동 관리
