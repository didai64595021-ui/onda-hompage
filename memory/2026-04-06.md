# 2026-04-06 일일 메모리

## 오늘 완료된 작업 (2026-04-05)

### 1. onda-ad 포인트계산기 "원" 오버플로우 수정
- 파일: `portfolio-sites/onda-ad/styles.css`
- 수정: `calculator-input`의 `max-width: calc(100% - 40px)` 제거, `calculator-suffix`에 `flex-shrink: 0` 추가
- 배포: GitHub Pages https://didai64595021-ui.github.io/onda-hompage/portfolio-sites/onda-ad/

### 2. claude --print 텔레그램 보고 누락 해결 (영구 적용)
- **원인**: claude --print 모드는 비대화형 → 내부에서 bash/notify 실행 불가
- **해결**: `/home/onda/scripts/claude-with-notify.sh` wrapper 생성
- **사용법**: `claude-with-notify.sh <project> <task_summary> <chat_id> --permission-mode bypassPermissions --print "태스크"`
- wrapper가 claude 완료 감지 → 자동으로 텔레그램 전송 (성공/실패/소요시간/커밋 포함)
- 모든 claude --print 직접 호출 → 이 wrapper로 대체 필수

### 3. onda-logic-monitor 대시보드 경쟁사 업체 대량 노출 버그 수정
- **증상**: 관리업체 34개인데 990개+ 노출
- **원인 1**: UTC 05시 크롤러 실행 중 4,161개 업체 `is_managed=true`로 잘못 INSERT
- **원인 2**: `crawler/adlog.ts`의 `batchMatchOrCreate`, `ensureBusiness` 함수에서 경쟁사도 businesses에 INSERT하는 경로 존재
- **원인 3**: `sync-from-adlog.ts`에서 keyword 조회 시 `.single()` 사용 → 중복 keyword 있으면 에러 → kw=null → 매 실행마다 새 keyword+business row 중복 생성
- **수정**:
  - DB 즉시 복원: 4,161개 → `is_managed=false`
  - 크롤러에서 경쟁사 INSERT 경로 완전 차단
  - `.single()` → `.limit(1)` 변경 + 비활성 keyword 재활성화 로직 추가
  - 중복 row 정리: 104개 → 36개 (AdLog 34항목 + 마드모아젤헤어 2키워드)
- **검증**: sync 3회 연속 실행 → 변경 0건 확인

### 4. 대시보드 관리업체 카운트 104 → 34 수정
- `stats.total`을 `dedupedRecs.length`(104) → `고유 place_id 기준`(34)으로 변경
- 카드는 키워드별로 유지 (1업체 2키워드 → 카드 2개)
- PM2 재시작 + git push 완료

### 5. 중곡동네일 is_managed 수정
- 이너네일(place_id=2048665748, biz_id=171812, keyword_id=8)의 keyword_id=8이 `is_managed=false`로 잘못 설정
- 수정 후 오늘 순위 54위 스냅샷 저장 완료

### 6. 순위 "-" 업체 현황
- **이너네일 / 중곡동네일**: ✅ 오늘 순위 54위 저장 완료
- **마드모아젤헤어 반곡점 / 나성동미용실**: ✅ 오늘 순위 11위 저장 완료
- **아비쥬의원 범계 / 범계 피부과**: ⏳ AdLog 서버 4/4~4/5 데이터 미업데이트 → 내일 크론(KST 16시) 자동 수집 예정

## 오늘 발견된 중요 사실

### onda-logic-monitor DB 구조
- `businesses` 테이블: place_id + keyword_id 조합으로 row (1업체 N키워드 = N rows)
- `is_managed=true` 정상 개수: **36개** (AdLog 34항목 + 마드모아젤헤어 반곡점 2키워드)
- 고유 업체 수(place_id 기준): **31~34개**
- `is_managed=false`인 중복 row가 60개+ 존재하므로 place_id 기반 매칭 시 주의 필요

### 크롤러 매핑 실패 원인
- `batchMatchOrCreate`에서 place_id로 매칭 시 is_managed=false row가 먼저 반환될 수 있음
- 코드에 `sort by is_managed desc` 정렬이 있어서 보통은 OK
- 하지만 managedIds 구성 시 keyword_id가 is_managed=false면 누락 → "업체 매핑 실패" 로그

### sync-from-adlog 정상 동작 확인
- 3회 연속 실행 → 변경 0건 (중복 방지 완전 동작)

## 내일 할 일
- 아비쥬의원 범계 크론 자동 수집 확인 (KST 16시 이후)
- 743건 특이사항 배너 내용 검토 (naver_purge_pattern 3건, Top5 탈락 11건 등)

## 환경 정보 (재확인)
- Supabase: byaipfmwicukyzruqtsj
- PM2 logic-monitor: port 3000 → nginx 8080
- AdLog 계정: onda / onda123 (환경변수 ADLOG_EMAIL/ADLOG_PASSWORD)
