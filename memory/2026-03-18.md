# 2026-03-18 일일 로그

## iwinv 서버 이전 완료 (00:30~00:44)
- **현재 서버: iwinv(49.247.137.28, 한국 SMILESERV, 인천)**
- **한국 서버** → 네이버/AdLog 프록시 없이 직접 접근 200 OK 확인
- 이전 핀란드 서버(89.167.101.152)는 해지 완료

### iwinv 서비스 구성 (최종)
| 서비스 | 포트 | PM2 이름 | 상태 |
|--------|------|----------|------|
| YouTube 대시보드 (Next.js) | 3000 → nginx :80 | onda-web | ✅ online |
| 로직 모니터 (Next.js 15.5.12) | 3001 → nginx :8080 | logic-monitor | ✅ online (신규 추가) |
| YouTube API | 4000 | onda-api | ✅ online |
| Upload Watchdog | - | upload-watchdog | ⏸️ stopped (CH03 토큰 만료) |
| OpenClaw | 18789/18791/18792 | - (systemd) | ✅ running |
| nginx | 80, 8080 | - (systemd) | ✅ enabled |
| onda-proxy | 3128 | - (systemd) | ✅ enabled |
| PM2 크론 18개 | - | cron-* | 스케줄 대기 |

### 접속 URL
- YouTube 대시보드: http://49.247.137.28 (포트 80)
- 로직 모니터: http://49.247.137.28:8080 (포트 8080)
- YouTube API: http://49.247.137.28:4000

### 완료한 작업
1. root PM2 중복 크론 18개 삭제 (onda user PM2와 중복)
2. cloudflared 설치 (2026.3.0) — 단, API 토큰에 터널 권한 없어서 미사용
3. nginx 설치 + 리버스 프록시 설정 (80→3000, 8080→3001)
4. logic-monitor PM2 등록 (포트 3001, Next.js 15.5.12)
5. PM2 startup systemd 등록 (서버 재부팅 시 자동 복구)
6. PM2 save 완료
7. 크론 4개 테스트 통과 (guarantee-check, contract-renewal, anomaly-detect, competitor-alert)
8. 네이버/AdLog 직접 접근 200 OK 확인 (프록시 불필요)

### 미해결
- **YouTube CH03 OAuth 토큰 만료 (invalid_grant)** — 5개 영상 업로드 대기 중, 관리자 재인증 필요
- upload-watchdog 중지시킴 (토큰 갱신 전까지)
- Cloudflare API 토큰에 터널 권한 없음 — 커스텀 도메인 HTTPS는 Let's Encrypt or 토큰 재발급 필요
- .env의 KOREA_PROXY_URL 아직 남아있음 — 한국 서버라 localhost 직접 접근으로 변경 가능

### 서버 리소스 (00:44 기준)
- 메모리: 1.2G/3.8G 사용, 2.4G 여유
- 디스크: 19G/49G 사용, 30G 여유
- CPU: 4코어

### 이전 서버 상태
- 이전 핀란드 서버(89.167.101.152) 해지 완료 — 더 이상 사용하지 않음

## 톡톡 활성업체 크롤링 시스템 (15:00~18:00)

### 요청
- 네이버 플레이스 톡톡 기능 활성화된 업체만 크롤링
- 톡톡 발송전용 영업용 (잠재고객 발굴)
- 수집 항목: 업체명, 주소, 전화, 업종, 지역, 홈페이지, 이메일, 플레이스URL, 톡톡활성여부

### 네이버 API 계정 현황
- **App 1 (onda-coldmail)**: Client ID `yoBUbNSW9MGSPH36zaHN` / Secret `wt0HPPOVKA` — 25,000/일
- **App 2 (onda-logic-monitor)**: Client ID `Su_kCP4chZNUyLO5wZEQ` / Secret `I4fA34bv0e` — 25,000/일
- **합계: 50,000건/일**
- 네이버는 사용량 조회 API 없음 → developers.naver.com 대시보드에서만 확인 가능

### 크롤링 아키텍처
- 스크립트: `/home/onda/projects/onda-coldmail/scripts/talktalk-crawl.js` (531줄)
- 결과: `/home/onda/projects/onda-coldmail/data/talktalk-results.csv` (UTF-8 BOM)
- 진행상태: `/home/onda/projects/onda-coldmail/data/talktalk-progress.json`
- 로그: `/home/onda/projects/onda-coldmail/data/talktalk-crawl.log`
- 프록시: `http://49.247.137.28:3100` (한국 프록시, `x-api-key: onda-proxy-2026-secret`)
- 톡톡 판별: `m.place.naver.com/place/{placeId}/home` HTML에서 `talk.naver` / `talktalkUrl` / `talkId` 검색
- Place ID 추출: `m.search.naver.com` 검색 → HTML에서 `place.naver.com/place/(\d+)` 패턴 추출

### 크롤링 진행 현황 (컴팩션 시점)
- API 호출: 62 / 25,000
- 프록시 요청: 59건
- 수집 업체: 31개
- **톡톡 활성: 27개 (87% 활성률)**
- 조합 진행: 6 / 464 (29업종 × 16지역)
- 지역: 서울~울산 치과까지 완료

### 다음 작업 (컴팩션 전 진행 중이던 것)
- **2개 API 키 번갈아 사용하도록 스크립트 수정 (한도 50,000으로)**
- **PM2에 등록하여 봇 세션 끊겨도 백그라운드 동작**
- 관리자 지시: "총2개계정 5만개 수집완료하고 봇 끊겨도 돌아가게 백그라운드 서버에 넣어"
- onda-logic-monitor .env에서 두 번째 API 키 이미 확인 완료
- 스크립트 수정 → PM2 등록 → 재시작 남음

### Git 커밋
- `b405120` feat: 톡톡 활성업체 크롤링 스크립트 추가 (pushed)
