# 2026-03-15 작업 로그

## onda-selfmarketing MVP 완성
- **레포**: didai64595021-ui/onda-selfmarketing
- **URL**: https://didai64595021-ui.github.io/onda-selfmarketing/
- **어드민**: admin.html (PIN: 0000)
- **총 규모**: 26페이지 HTML + 36 Supabase 테이블 (sm_ prefix) + 16 RPC 함수
- **기술**: 순수 HTML/CSS/JS (프레임워크 없음), Supabase 실시간 연동, GitHub Pages 배포
- **Supabase**: 기존 프로젝트 byaipfmwicukyzruqtsj 재사용 (onda-logic-monitor와 동일 DB)

### 기획서 2개 전체 기능 구현 완료
- 영업전략 v10 + 로직 인텔리전스 v10 문서 기반
- 핵심 페이지: hotleads(87K 발굴), messages(A/B/C 템플릿), customers(7단계 생애주기), kakao(자동응답 시퀀스), trial-sequence(D+N 자동), referral, agency, community(시딩), targets(DB고갈모니터링), kpi(일/주/월 목표+위험신호), payments(결제실패 자동대응+환불산출), keywords(확장+자기잠식+교차효과), vendor, remarketing, analytics, hypotheses, constraints, leaderboard, events, audit, decisions, admin
- DB 17개 추가 생성: sm_kakao_sequences, sm_trial_sequences, sm_referrals, sm_agencies 등
- RPC 6개 추가: create_kakao_sequence, handle_payment_failure, calculate_refund, detect_cannibalization 등

### 남은 작업
- 카카오 채널 URL 미제공 → #KAKAO_URL 플레이스홀더 상태
- admin PIN 0000 → 보안 강화 필요
- 7개 자동화 토글 → 실제 Cron/Edge Function 구현 필요
- RLS anon 오픈 상태 → 프로덕션 전 제한 필요
