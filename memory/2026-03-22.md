# 2026-03-22 일일 로그

## 네이버 플레이스 크롤러 GUI 개선 (onda-coldmail, 14:00~16:30)

### 완료된 작업
1. **네이버ID @naver.com 형식 적용** — `extract-derma-v2.js` line 114 수정, 커밋 `05e5df2`
2. **한글 인코딩 깨짐 수정** — `resp.encoding = "utf-8"` 강제 설정 (프록시 응답 인코딩 오보고 문제), 커밋 `e8014ae`
3. **중복 업체 자동 제거** — 고유번호(Place ID) 기준 중복 체크, 커밋 `0da4469`
4. **네이버 API 키 기본 내장** — App 1 (onda-coldmail) 키를 crawler_engine.py에 내장, 커밋 `2b342f5`
5. **파이프라인 방식 적용** — 페이지 수집 즉시 상세 크롤링 (대기 없음), 커밋 `692fd10`
6. **5가지 속도 최적화** — 커밋 `d467909`
   - 연결 풀 20개 (HTTPAdapter pool_connections=20)
   - 이메일 폴백 검색 제거 (홈페이지+place HTML만)
   - 네이버ID 4→2단계 폴백 (1단계 성공률 90%+)
   - HTML 파싱 정규식 통합
   - place HTML에서 이메일 직접 추출
7. **진행 복구 키워드 체크** — 다른 키워드로 재시작 시 이전 progress 파일 자동 삭제
8. **매건 실시간 저장** — 10건마다 → 매건으로 변경

### 속도 개선 결과
| 버전 | 10건 소요 | 업체당 |
|---|---|---|
| 최초 (v10) | 170초 | 17초 |
| 파이프라인 | 44초 | 4.4초 |
| 5가지 최적화 | 38초 | **~2초** |

### 사용자 Windows 테스트 이슈
- PowerShell에서 폴더명 공백 → 따옴표 필요 (`cd "폴더 이름"`)
- 수정 전 버전으로 테스트 → 한글 깨짐 + 네이버ID 0% (상세 크롤링 전 중지)
- `pip install`은 처음 1번만, 이후 `python fill_crawl_gui.py`만 실행

### 파일 위치
- GUI: `scripts/gui/fill_crawl_gui.py`, `scripts/gui/crawler_engine.py`
- Playwright 스크립트: `scripts/extract-derma-v2.js`
- 최신 커밋: `d467909` (perf: 5가지 속도 최적화)
- GitHub: `didai64595021-ui/onda-coldmail`

### 핵심 기술 메모
- **한글 이중 인코딩**: 프록시가 UTF-8을 Latin-1로 잘못 디코딩 → `resp.encoding = "utf-8"` 강제 설정으로 해결
- **중복 원인**: HTML 폴백에서 네이버 검색 페이지가 같은 업체를 여러 페이지에 걸쳐 반환
- **place HTML 2회 fetch 문제**: STEP 2(홈페이지)와 STEP 4(네이버ID)에서 각각 fetch → 1회로 통합
- **파이프라인**: `page_rows` 수집 후 `rows.extend` 대신 즉시 `_detail_crawl_one()` 호출

## 추가 개선 (16:30~16:55)

### 완료된 작업
9. **네이버 지도 API 추가** — `map.naver.com/p/api/search/allSearch` 30건/페이지 + ncaptcha 감지 + HTML 폴백, 커밋 `3912fc9`
10. **HTML 스트리밍 파싱** — Place ID 발견 즉시 연결 끊기 (페이지 수집 17초→0.6초), 커밋 `86025c6`
11. **GUI 종료 시 임시파일 자동 삭제** — progress + temp 파일 X 버튼 시 자동 정리, 커밋 `703c411`
12. **Playwright map.naver.com 브라우저 크롤링 적용** — 커밋 `84c0afa`
    - 크롤링 순서: Playwright → Map API → HTML 폴백 3단계
    - API 응답 가로채기 (`page.on("response")`) — allSearch JSON 직접 파싱
    - 30건/페이지 페이지네이션 지원
13. **Playwright 핑거프린트 우회** — 커밋 `fa7500e`
    - UA 랜덤 로테이션 (13개 중 선택)
    - `navigator.webdriver` 숨김
    - 랜덤 viewport (1280~1920 x 720~1080)
    - `navigator.languages = ['ko-KR', 'ko', 'en-US', 'en']`
    - `navigator.platform = 'Win32'`

### 속도 개선 (업데이트)
| 버전 | 10건 소요 | 업체당 |
|---|---|---|
| HTML 스트리밍 | 35초 | ~2초 |

### 서버 차단 상태
- **서버 IP ncaptcha 차단**: `map.naver.com/p/api/search/allSearch`와 `pcmap-api.place.naver.com/graphql` 모두 429
- **map.naver.com HTML**: CSR 전용 (2KB 빈 껍데기), 직접 파싱 불가
- **localhost:3100 프록시**: JS 렌더링 미지원, map.naver.com에서 Place ID 추출 불가
- **Windows PC에서만 테스트 가능** — 사용자 PC IP는 차단되지 않을 가능성

### Playwright 적용 핵심 결정
- `_playwright_search()` → `_graphql_search(Map API)` → `_html_search_fallback()` 3단계 체인
- Playwright는 실제 브라우저(Chromium)로 map.naver.com 접속, API 응답을 가로채서 JSON 파싱
- 페이지 2+ 진입 시 searchIframe 내 페이지 버튼 클릭
- 브라우저 인스턴스 재사용 (`CrawlerEngine._pw_browser` 클래스 변수)
- `_close_playwright()` — GUI 종료 시 호출

### 사용자 Windows 설치 완료
- `pip install playwright` + `playwright install chromium` 완료
- Chromium v1208 (Chrome 145.0.7632.6) 다운로드 완료
- 사용자 경로: `C:\Users\user\AppData\Local\ms-playwright\chromium-1208`
- **테스트 결과 대기 중** — Playwright가 실제로 30건/페이지 수집하는지 확인 필요

### 최신 커밋
- `fa7500e` — feat: Playwright 핑거프린트 우회
- GitHub: `didai64595021-ui/onda-coldmail`

## 다중 수집 전략 + 속도 최적화 (17:00~18:38)

### 핵심 발견: m.map.naver.com/search2 엔드포인트
- URL: `https://m.map.naver.com/search2/search.naver?query={kw}&sm=hty&style=v5`
- **75건/페이지** (기존 API 5건, Map API 30건 대비 8배)
- 서버 차단 없음 (ncaptcha 없음), Playwright 불필요
- `__RQ_STREAMING_STATE__`에 JSON 데이터 포함 (id, name, tel, address, totalCount 등)

### 동 단위 자동 분할 구현
- **행정동 API**: `https://grpc-proxy-server-mkvo6j4wsq-du.a.run.app/v1/regcodes?regcode_pattern={code}*`
  - 시도 코드: 서울=11, 부산=26, 대구=27, 인천=28, 광주=29, 대전=30, 울산=31, 세종=36, 경기=41 등
  - 구/군 → 동/읍/면 단위까지 자동 취득 (하드코딩 불필요)
- 수집 흐름: 원본 키워드(75건) → 구/군 분할(+N건) → 동 단위 분할(+N건) → 중복 제거
- **울산 피부과**: 568건 중 579건 수집 (102%), 중복 0개
- **수원 피부과**: 1,176건 중 1,212건 수집 (103%), PHASE 1 = 62초

### 속도 최적화 (커밋 `9abe950`)
- 딜레이: 3~8초 → **0.5~1.5초**
- timeout: 8초 → **5초**
- 홈페이지 fetch timeout: 4초 → 3초
- JSON 캐시: m.map.naver.com에서 기본정보(name, tel, addr, category) 미리 추출 → place HTML fetch 절감
- `CrawlerEngine._place_info_cache` 클래스 변수에 PID별 캐싱

### PHASE 2 예상 속도
| 건수 | 이전 (3~8초) | 이후 (0.5~1.5초) |
|---|---|---|
| 200건 | ~15분 | ~5분 |
| 500건 | ~40분 | ~12분 |
| 1,000건 | ~80분 | ~25분 |

### 프록시 필요 여부 결론
- 현재 m.map.naver.com + m.place.naver.com = 일반 웹페이지 요청 → **밴 안 먹힘**
- 하루 ~1,000건: 프록시 불필요
- 하루 ~5,000건: 위험할 수 있음
- **하루 3만건: 프록시 필요** → 하이아이피(haiip.net) 대기 중
- 403/429 감지 → 자동 IP 교체 로직 이미 구현 (proxy-pool.js)

### 커밋 이력
- `2382988` — Playwright 에러 로그 GUI 표시
- `a39e090` — 다중 수집 전략 (PHASE 1/2 분리)
- `1e49686` — 모바일 지도 검색 (75건/페이지)
- `1b5e662` — 구/군 분할 페이지네이션 (202건+)
- `403b5bb` — 동 단위 자동분할 (579건, 102%)
- `9abe950` — 속도 최적화 (딜레이 0.5~1.5초, JSON캐시)

### 메서드 목록 (최신)
- `_mobile_map_search_single(keyword)` — m.map.naver.com 단일 검색 (75건)
- `_mobile_map_search(keyword)` — 구/동 자동 분할 + 중복 제거 (98%+)
- `_fetch_sub_districts(city_name)` — 행정동 API 동적 취득
- `_place_info_cache` — PID별 JSON 기본정보 캐시 (클래스 변수)

### 파일 위치
- `scripts/gui/crawler_engine.py` — 메인 엔진
- `scripts/gui/fill_crawl_gui.py` — tkinter GUI
- GitHub: `didai64595021-ui/onda-coldmail`, branch: main

## 전국 행정구역 오프라인 데이터 + GUI 리팩토링 (21:00~22:30)

### 완료된 작업
1. **전국 행정구역 오프라인 데이터** — 커밋 `913d154`
   - `districts_data.json`: 17개 시도, 262개 구/군, 20,375개 동/읍/면
   - 행정동 API 호출 제거 → JSON 파일에서 직접 조회 (AI/API 불필요)
   - `_load_districts_data()` classmethod + `_DISTRICTS_DATA` 클래스 캐시
   - `_CITY_TO_GU` 매핑 추가 (안양→만안구/동안구, 수원→장안구/권선구/팔달구/영통구 등 13개 시)
   - 강원도 코드 변경 반영 (42→51, 강원특별자치도)
   - 하위 구 없는 지역(서면 등) → city 전체 분할 자동 폴백

2. **큐 자동 진행 버그 수정** — 커밋 `3b4361d`
   - 원인: 각 키워드 완료 시 `done` 이벤트 → `messagebox.showinfo` 팝업이 블로킹
   - 수정: `_queue_running=True`일 때 done 팝업 차단, 전체 완료 시에만 팝업

3. **CSV 모드 제거, 키워드 큐 통일** — 커밋 `9a1b876`
   - 입력 모드 세그먼트 버튼 (CSV/키워드) 제거
   - 키워드 큐 하나로 통일, 큐 비어있으면 입력창 키워드로 단일 실행
   - `_toggle_keyword_widgets`, `_on_input_mode_change`, `_browse_input` 메서드 삭제
   - config에서 `input_mode`, `last_input` 키 제거

4. **반응형 가로 레이아웃** — 커밋 `50a3791`
   - 기본 1100x650, 최소 700x500
   - 1000px+: 좌측(입력+큐+설정) | 우측(통계) 2컬럼
   - 800px 미만: 세로 1컬럼 자동 전환
   - `<Configure>` 이벤트 + 80ms 디바운스

5. **GraphQL API 상세 크롤링** — 커밋 `97379a2`
   - `_fetch_place_graphql(pid)` — pcmap-api.place.naver.com/graphql
     - 전화번호(phone/virtualPhone), 홈페이지(repr.url), 방문자리뷰, 카테고리, 주소
   - `_fetch_place_summary(pid)` — map.naver.com/p/api/place/summary/{pid}
     - blogReviews.total, visitorReviews.displayText에서 숫자 추출
   - PHASE 2 우선순위: GraphQL → Summary → place HTML → 홈페이지 이메일 추출
   - 네이버아이디: GraphQL homepage가 blog.naver.com이면 ID 자동 추출

### GraphQL 스키마 발견 (서버 introspection 차단, 브루트포스로 확인)
- `placeDetail(input: PlaceDetailInput!)` → `base`, `homepages`, `fsasReviews`
- `base`: name, phone, virtualPhone, category, address, roadAddress, visitorReviewsTotal, visitorReviewsScore
- `homepages`: `repr { url type }`, `etc { url type }`, `subLinks { url title }`
- `fsasReviews`: total (이건 블로그 리뷰가 아님, summary API의 blogReviews.total이 정확)
- `Homepage.__typename` = "Homepage", `repr.__typename` = "HomepageRepr"

### 서버 IP 차단 상태
- `pcmap-api.place.naver.com/graphql` — 429 차단 (서버 IP)
- `map.naver.com/p/api/place/summary/{pid}` — 200 정상 (차단 없음)
- `m.map.naver.com/search2` — 200 정상 (차단 없음)
- **사용자 Windows PC에서는 GraphQL도 정상 작동할 것**

### 사용자 Windows 파일 세트 (HighTech PC)
- `C:\Users\HighTech\Downloads\새 폴더\` 에 3개 파일:
  1. `fill_crawl_gui.py` — GUI (반응형 가로, 키워드 큐 통일)
  2. `crawler_engine.py` — 엔진 (GraphQL + Summary + 오프라인 행정구역)
  3. `districts_data.json` — 전국 행정구역 (필수)
- 실행: `C:\Users\HighTech\AppData\Local\Programs\Python\Python314\python.exe fill_crawl_gui.py`

### 커밋 이력 (최신순)
- `97379a2` — feat: GraphQL API 상세 크롤링 + summary API 폴백
- `50a3791` — design: 반응형 가로 레이아웃
- `9a1b876` — refactor: CSV 모드 제거, 키워드 큐 통일
- `3b4361d` — fix: 큐 모드 팝업 차단
- `913d154` — feat: 전국 행정구역 오프라인 데이터

## 네이버ID 파싱 수정 + 150건 대조 검증 + 가로 레이아웃 수정 (22:30~23:47)

### 완료된 작업
1. **네이버ID 하이픈(-) 포함 파싱 수정** — 커밋 `bec1752`
   - 정규식 `[a-zA-Z0-9_]+` → `[a-zA-Z0-9_.-]+`
   - dr-lim2015, medi-click, e-yedam 등 정상 추출
   - `blogId=` 파라미터 우선 파싱, `profile`/`intro` 시스템 경로 제외
   - `_SKIP` 목록: PostView, PostList, BlogTagLog, NBlogTop, prologue, MologPost, profile, intro

2. **크롤링 결과 150건 대조 검증** — 방문자리뷰/블로그리뷰 수치 100% 일치 확인
   - 이메일: 2/150건(1.3%) — 홈페이지 이메일 노출 자체가 드묾
   - 중복 NID: binterest, g-bean (같은 블로그가 여러 업체 운영)
   - 오추출 3건(dr, medi, profile) → 커밋 `bec1752`에서 수정

3. **가로 레이아웃 통계 카드 잘림 수정** — 커밋 `c97e7b3`
   - 기본 크기 1200x700 (기존 1100x650), 최소 800x500
   - 가로 전환 임계값 1000px → 800px
   - 통계 카드: 3열x2행 → 2열x3행 (우측 패널 좁을 때 잘림 방지)
   - 통계 카드 폰트 축소: FONT_STAT_NUM 24→18, FONT_STAT_LABEL 9→8
   - 아이콘 폰트 16→13, 패딩 축소
   - content columnconfigure: weight 5:3, minsize 400:300
   - `uniform="stat"` 적용으로 균등 분배

### 최신 커밋 이력
- `c97e7b3` — fix: 가로 레이아웃 통계 카드 잘림 수정
- `bec1752` — fix: 네이버ID 하이픈(-) 포함 파싱 + profile/intro 제외
- `97379a2` — feat: GraphQL API 상세 크롤링 + summary API 폴백

### 사용자 파일 경로
- Primary PC: `C:\Users\user\Desktop\비즈니스\네이버 크롤러`
- Secondary PC (HighTech): `C:\Users\HighTech\Downloads\뉴`
- HighTech Python: `C:\Users\HighTech\AppData\Local\Programs\Python\Python314\python.exe` (PATH 미설정)

### 핵심 규칙 (사용자 명시)
- **"되는 로직은 건들지말고 안되는거만 바꾸자"** — 작동하는 로직 수정 금지
