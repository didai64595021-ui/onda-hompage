# 2026-03-13 작업 로그

## 완료 작업

### 블리독 의정부점 중복 수정
- 중복 business (id:150763, 150764) 비활성화, 원본(id:614) 재활성화
- 중복 계약 취소, 중복 키워드(109,110) 비활성화

### 중복 방지 로직 구현
- `app/api/admin/businesses/route.ts`: place_id URL 파싱, 키워드 dedup, 비활성 업체 재활성화
- `app/api/admin/data-integrity/route.ts`: 중복 감지 API (신규)
- 키워드 text `trim()` 정규화 적용

### 테스트 데이터 정리
- 11개 테스트 업체(150749-150762) `is_managed=false` 처리
- 11개 테스트 키워드 + 중복 "상도동헬스장"(91) 비활성화

### 경쟁사 N2 비교 로직
- `app/api/cron/traffic-efficiency/route.ts`에 추가
- ⚠️ migration.sql 미실행 (competitor_avg_n2_change, competitor_gap 컬럼)

### 블로그 리뷰 백필
- 3/2~3/10 null 원인: direct-crawl.js(3/2 폐기) → adlog crawler로 전환 시 누락
- `scripts/backfill-reviews.ts` 실행 완료, 56+57건 업데이트

### 중복 키워드/업체 수정
- keyword_id=54 (영등포피부과 중복) → 비활성화
- business_id=2071 (블리비 영등포 잘못된 place_id) → 비활성화

### 계약 만료 수동 전환
- `getEffectiveStatus()` 자동 expired 제거, `isContractExpired()` 헬퍼 분리
- UI에 ⚠️ 배지 + "계약종료"/"재계약" 버튼

### place_id 수정 (4건)
- 616 갓잇위례(→1141251217), 619 톡톡구강내과(→31045048), 620 블리비하남미사(→1779845334), 624 블리비안양(→1228888051)

### AdLog vs DB 데이터 검증
- 8/8 관리업체 일치 확인 (`scripts/verify-data.ts`)

## 미완료

### AdLog OCR 검증 (Playwright)
- 로그인 성공, 키워드 검색 완료, 검색결과 스크린샷 촬영
- **미해결**: 검색결과→상세 순위테이블 이동 URL/클릭 타겟 미확정
- 스크린샷: `/home/onda/projects/onda-hompage/adlog_*.png`

### 기타 미완료
- migration.sql Supabase 수동 실행 필요
- onda-logic-monitor MEMORY.md에 작업 응답 3원칙 저장 안 됨
- 갓잇 위례 급락(48→178위) 분석 필요
