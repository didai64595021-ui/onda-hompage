# 2026-02-26 (수요일)

## 관리자 지시사항 (중요 - 영구 적용)

### 1. 스텝별 자동 보고 (모든 프로젝트)
- 작업 시작 → "🔨 작업 시작: [내용]"
- 스텝 완료 → "✅ [스텝명] 완료: [결과]"
- 오류 발생 → "⚠️ 오류: [내용] → 복구 중..."
- 복구 완료 → "🔄 복구 완료, 재진행"
- 배포 완료 → "🚀 배포 완료: [링크]"
- **한다고 한 건 끝나면 반드시 결과 보고 (재촉 안 해도)**

### 2. 오류 시 자동 이어서 진행 (모든 프로젝트 봇)
- 오류로 멈추면 **직전 작업 내용 기억해서 알아서 이어서 진행**
- 멈춘 채로 방치 금지 → 자동 복구 후 재개
- 보고는 당연히 하되, 보고만 하고 멈추지 말 것

### 3. 작업 완료 후 테스트+디버깅 무한 반복 (모든 프로젝트 공통) ⭐최우선
- 작업 종료 후 반드시 **빌드 테스트 → 오류 수정 → 재테스트** 무한 반복
- 오류 0개가 될 때까지 계속 반복 — 중간에 멈추지 말 것
- 테스트 통과해야만 "완료" 보고
- 이 규칙은 모든 프로젝트, 모든 작업에 적용
- **디버깅 → 테스트 → 디버깅 → 테스트 사이클을 공통 로직으로 내재화**
- 코딩 에이전트에 작업 위임 시에도 이 사이클 반드시 포함하여 지시

### 4. 봇 간 작업 중복 방지 — 글로벌 워크로그 (필수, 모든 봇 공통)
- **경로: `/home/onda/projects/GLOBAL_WORK_LOG.md`**
- 모든 봇은 매 세션 시작 시 이 파일 확인
- 작업 시작하면 기록, 끝나면 상태 업데이트
- 다른 봇이 진행 중인 작업 절대 중복 금지
- 병렬 작업 시 각자 다른 스텝 담당 + 인터페이스 미리 합의
- 새 프로젝트 생성 시에도 AGENTS.md에 글로벌 워크로그 참조 자동 포함

### 5. 오류/디버깅 능동 해결
- 오류 발생 시 관리자에게 물어보지 말고 **스스로 최선의 방법 찾아서 해결**
- Claude Code API 안 되면 직접 코딩
- API 서버 장애면 대안 찾기 (작업 쪼개기, 직접 수정 등)
- 항상 능동적으로 디버깅
- **"안 된다"고 보고만 하지 말고, 해결 후 보고**

### 6. 토큰/API키 노출 방지 (모든 프로젝트 공통) ⭐필수
- git push, 배포, 파일 공유 전 **반드시 민감정보 검수**
- .env, API 키, 토큰, 비밀번호가 코드/커밋에 포함되지 않았는지 확인
- `.gitignore`에 `.env`, `.env.local`, `credentials`, `*.key` 등 반드시 포함
- 커밋 전 `git diff --cached`로 민감정보 유출 체크
- 채팅에 키/토큰 절대 출력 금지 (마스킹 처리)
- 실수로 커밋된 경우 즉시 히스토리에서 제거 + 키 로테이션 권고

### 7. 메시지 누락 방지 (모든 프로젝트 공통) ⭐최우선
- 수신한 메시지는 **절대 무시하지 않는다** — 작업 중이면 "작업 중..." 이라도 응답
- 여러 메시지 동시 수신 시 **각각 순서대로 처리** (하나도 빠뜨리지 않음)
- Claude Code 작업 완료 후, 작업 중 들어온 미처리 메시지 확인 → 순서대로 처리
- 3분 이상 작업 시 중간 진행 보고 (1~2분 간격)
- 사용자 질문/확인 메시지는 작업 중이어도 즉시 응답

## onda-hompage 작업 로그

### 홈페이지 3개 버전 (새벽 1~3시)
- Type A 🟢 라이트 그린: https://type-a-green.vercel.app (7페이지)
- Type B 🖤 사이버펑크: https://type-b-iota.vercel.app (7페이지, 로봇보관소 로딩 애니메이션)
- Type C 🏆 골드 럭셔리: https://type-c-rouge.vercel.app (7페이지)
- 각 버전: index, about, services, portfolio, blog, contact(카카오채널), faq
- 초록(#00C853) 시그니처 컬러
- 사업자정보: 대표 황승현, 739-40-01011, ondadaad@google.com, 강남구 테헤란로82길 15 디아이타워 3층 4호
- 카카오채널: http://pf.kakao.com/_xdbJnn/chat

### 수정사항
- Type B 푸터: 터미널 스타일 → 4컬럼 그리드 레이아웃
- Type B/C 헤더: 서브페이지 링크 + 사업자정보 푸터 추가
- 로고/햄버거 상하 중앙 정렬 통일 (7페이지 전부)
- 크로스디바이스 반응형 보강 (280px Galaxy Fold ~ 4K 3840px)
- word-break:keep-all, 터치타겟 44px+, safe-area
- contact.html → 카카오채널 유입 방식으로 변경 (폼 제거)
- Type B 포트폴리오에 관리자 사진(회/김밥) 추가

### 박재훈 참고사이트 기반 초안 10개 (저녁)
- 참고: unideca.com, growthpartner.co.kr, theoreum2020.com, growthplan.kr, einsmedia.co.kr, techlabs.co.kr
- 1차 5개 배포 완료:
  1. 신뢰 숫자 중심: https://draft-1-beige.vercel.app
  2. 프리미엄 브랜딩: https://draft-2-five.vercel.app
  3. 데이터 퍼포먼스: https://draft-3-gray.vercel.app
  4. 프로세스 랜딩: https://draft-4-five.vercel.app
  5. 스토리텔링 원페이저: https://draft-5-plum.vercel.app
- 2차 5개 (6~10) 작업 중: SaaS, 문제-해결, 포트폴리오, 전환최적화, 매거진

### 팀 멤버
- 관리자: 000 (@gumibear012, ID: 7383805736)
- 직원: 박재훈 (@rkawk104, ID: 8473911920)

### 핵심 교훈
- 서브에이전트 HTTP 500 에러 빈발 → 될 때까지 자동 재시도 필수
- 에이전트 에러나도 파일은 이미 작성된 경우 많음 → 파일 확인 후 커밋+배포
- 배포 완료 시 반드시 링크 포함하여 보고
- 관리자 "될 때까지 자동으로 해" 지시 → 영구 적용

### TTS 비활성화 (관리자 지시)
- Fish Audio TTS 기능 비활성화 (다시 넣으라 할 때까지)
- 파이프라인에서 TTS 스텝 스킵 처리
